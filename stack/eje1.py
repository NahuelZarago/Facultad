#1. Determinar el número de ocurrencias de un determinado elemento en una pila.
import stack



ocurrencia = stack.Stack()
pila = [1, 2, 3, 4, 5, 6, 7, 3, 3]

for i in pila:
    ocurrencia.push(pila(i))

def ocurrencias(o):
  for i  in pila:
    aux = stack.Stack()
    contador = 0
    while pila.size() > 0:
      dato = pila.pop()
      if dato == valor:
        
